# WebRTC のステップ解説

このドキュメントでは、このプロジェクトの各 step が何をしているのか、そしてどのように段階を踏んで WebRTC を学んでいくのかを説明します。

---

## step-01

フォルダ: `step-01`

### 何をしているのか
* `navigator.mediaDevices.getUserMedia()` を使ってローカルのカメラ映像を取得する。
* ブラウザ上の `<video>` 要素にその映像を表示する。
* WebRTC の最初のステップである「メディアの取得」を体験する。

### 学ぶポイント
この段階では、相手との接続はまだ行わず、あくまで自分のカメラを使うことだけを学びます。

### 実際の流れ
ブラウザがカメラ使用の許可を求め、許可されるとそのストリームが `localVideo.srcObject` に割り当てられます。

### 結果
自分の動画は見えるようになりますが、相手との接続、シグナリング、リモートストリームの受信はまだありません。

---

## step-02

フォルダ: `step-02`

### 何をしているのか
* `RTCPeerConnection` を使ってピア間接続を作る。
* Offer と Answer を生成する。
* 2 つのブラウザ間でビデオをやり取りする。

### 学ぶポイント
このステップで WebRTC の基本フローを学びます。
* ローカルストリームを取得する
* ピア接続を作る
* Offer を作る
* Answer を返す
* ICE candidate を交換する

### 実際の流れ
各側で `RTCPeerConnection` オブジェクトを作り、セッション説明（SDP）を交換することで接続が成立します。

### 結果
自分のローカル映像と、相手側のリモート映像を横に並べて見ることができます。

---

## step-03

フォルダ: `step-03`

### 何をしているのか
* 映像だけでなく `RTCDataChannel` を使って文字データをやり取りする。
* 相手同士でテキストを送受信する。
* WebRTC が動画以外のデータも扱えることを示す。

### 学ぶポイント
このステップは、メディア配信だけではなく「データ通信」の考え方も学ぶためのものです。

### 実際の流れ
送信用チャネルと受信用チャネルを作り、`send()` でデータをやり取りします。ブラウザのコンソールでは接続状態やメッセージイベントも確認できます。

### 結果
2 人のピア間で、簡単な文字メッセージを WebRTC のデータチャネル経由でやり取りできます。

---

## step-04

フォルダ: `step-04`

### 何をしているのか
* Socket.IO を使ったシグナリングサーバーを導入する。
* 複数のブラウザが同じ room に参加できるようにする。
* 多くの WebRTC アプリで使われる room ベースの接続フローを実現する。

### 学ぶポイント
このステップでは、異なる端末同士で SDP や ICE candidate を交換する仕組みを作ります。

### 実際の流れ
サーバーが `create or join` リクエストを受け取り、room の参加者数を管理します。2 人が同じ room に入ると、双方が WebRTC のハンドシェイクを開始できる状態になります。

### 結果
この段階から、実際の異なる端末間通信として WebRTC を使うことが現実的になります。

---

## step-05

フォルダ: `step-05`

### 何をしているのか
* room ベースのシグナリングを使って 2 人のビデオ通話を実装する。
* socket 経由で Offer / Answer / ICE candidate を交換する。
* リモートストリームの受信と通話終了処理を追加する。

### 学ぶポイント
このステップが、プロジェクトの中で最初の「本格的なリアルタイム動画通話」の例になります。

### 実際の流れ
クライアントは `got user media` を送信し、その後 SDP や ICE メッセージをやり取りします。2 人目の相手が同じ room に入ると接続が作られ、リモートストリームがビデオ要素にアタッチされます。

### 結果
同じ room に入った 2 つのブラウザ間で、実際のピアツーピアのビデオ通話ができます。

---

## step-06

フォルダ: `step-06`

### 何をしているのか
* 通話に加えて、画像転送用のデータチャネルを使う。
* カメラで写真を撮って相手に送り、相手側で表示する。
* WebRTC を「ただの動画通話」ではなく、より実用的なアプリとして扱う例を示す。

### 学ぶポイント
このステップでは、データチャネルが画像のピクセルデータやバイナリデータを送れることを学びます。

### 実際の流れ
画像データをチャンク単位に分割して `RTCDataChannel` で送信し、受信側で再組み立てて canvas に描画します。

### 結果
相手側で撮影した写真が表示され、より実際のチャットや共同作業アプリに近い動作になります。

---

## 各ステップの関係

このプロジェクトは次の順番で進んでいきます。

1. ローカルのメディアを取得する
2. WebRTC でピア同士をつなぐ
3. データチャネルでデータを交換する
4. room ベースのシグナリングを追加する
5. 完全なビデオ通話を作る
6. 実用的なアプリとして写真共有まで行う

つまり、最初は単なるカメラ表示のデモから始まり、最終的には異なる端末間で通信できる仕組みへと発展していきます。

---

## 実際に確かめるための使用方法

ここでは、実際に WebRTC を確認するための使い方を説明します。基本的には、同じネットワーク内にある 2 台のパソコンやブラウザを使って確認します。

### 1. 2 台のパソコンを用意する

最低でも次の 2 つが必要です。

* 1 台目: 通話を開始する側
* 2 台目: 通話を受ける側

同じ Wi‑Fi に接続している状態が理想です。

### 2. サーバーを起動する

プロジェクトのルートで次のように起動します。

```bash
npm install
node step-04/index.js
```

または、step-05 / step-06 で必要な場合は、そのフォルダ内の `index.js` を実行してください。

### 3. ブラウザでアクセスする

PC 1 と PC 2 のブラウザで同じ URL にアクセスします。

```text
http://<PCのローカルIPアドレス>:8080
```

例:

```text
http://192.168.0.10:8080
```

2 台とも同じ room 名を入力すると、相互に接続が試みられます。

### 4. 同じ room 名を使う

例として、どちらも `roomA` と入力します。

* 1 台目が room を作成する
* 2 台目がその room に参加する

この状態で、WebRTC のシグナリングが成立し、接続が開始されます。

### 5. 通話やデータ交換を確認する

- `step-02` では 2 つのブラウザ間で映像が流れることを確認する
- `step-03` では文字メッセージの送受信を確認する
- `step-05` ではビデオ通話が成立することを確認する
- `step-06` では写真を撮って相手へ送ることを確認する

### 6. 注意点

* 同じネットワーク内で試すのが基本です
* カメラやマイクのアクセス許可をブラウザで許可する必要があります
* `localhost` だけではなく、端末間通信の確認には IP アドレスでアクセスするのが実際的です
* ネットワーク環境によっては NAT / ファイアウォールの影響で接続がうまくいかないことがあります

### 7. もし異なる端末で試したい場合

同じラップトップや同じ Wi‑Fi の中で試すのが最も簡単です。

また、実運用では STUN / TURN サーバーを使って NAT を越える必要があります。これは WebRTC の実践では重要です。

---

## まとめ

このリポジトリは、単なるサンプル集ではなく、WebRTC の中核概念を順番に学ぶための教材です。

* メディアの取得
* ピア接続の作成
* SDP と ICE の交換
* サーバーを使ったシグナリング
* WebRTC によるデータ転送
* リアルタイムなマルチメディア通信

`step-06` まで進むと、これは単なるチュートリアルの例ではなく、実際の Web 通信アプリに近い動きを持つようになります。

さらに、2 台のパソコンで同じ room 名を使い、同じネットワークに接続して実際に動かすことで、WebRTC の仕組みを体感できます。
